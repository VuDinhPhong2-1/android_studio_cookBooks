<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="190dp"
    android:layout_height="200dp"
    android:layout_marginTop="10dp"
    android:layout_marginEnd="10dp"
    android:background="@drawable/back_ground_relative">

    <TextView
        android:id="@+id/btn_detail_food"
        android:layout_width="match_parent"
        android:layout_height="match_parent" />

    <ImageView
        android:id="@+id/image_food"
        android:layout_width="match_parent"
        android:layout_height="100dp"
        android:background="@drawable/back_gr_image"
        android:clipToOutline="true"
        android:scaleType="fitXY"
        android:src="@drawable/kfc" />

    <ImageView
        android:id="@+id/avatar"
        android:layout_width="20dp"
        android:layout_height="20dp"
        android:layout_below="@+id/image_food"
        android:layout_marginStart="10dp"
        android:layout_marginTop="10dp"
        android:background="@drawable/avatar_back"
        android:clipToOutline="true"
        android:src="@drawable/kfc" />

    <TextView
        android:id="@+id/name_user"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignBottom="@id/avatar"
        android:layout_marginStart="10dp"
        android:layout_toRightOf="@id/avatar"
        android:text="Huyen Le Tran"
        android:textColor="#F0F0F0" />

    <TextView
        android:id="@+id/name_food"
        android:layout_width="150dp"
        android:layout_height="wrap_content"
        android:layout_below="@+id/avatar"
        android:layout_marginStart="10dp"
        android:layout_marginTop="10dp"
        android:layout_marginEnd="20dp"
        android:layout_marginBottom="20dp"
        android:text="Canh cải bó xôi cá rô đồng"
        android:textColor="#F0F0F0"
        android:maxLines="2"
        android:ellipsize="end"
        android:textStyle="bold" />

</RelativeLayout>